# redis.conf

bind 0.0.0.0

port 6379

requirepass redis

# Включение AOF для постоянного сохранения данных
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# Указание директории для хранения AOF файла
dir /data
